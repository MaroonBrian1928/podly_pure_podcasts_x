You are aligning podcast chapter START times to the exact words spoken in a transcript.

For each chapter below, find the short, distinctive phrase in the transcript where that chapter actually begins, and report the transcript segment that contains it. We convert your phrase into an exact timecode using word-level timing, so the phrase MUST be copied verbatim from a segment.

CHAPTERS (with their current approximate start time, in seconds):
{% for chapter in chapters -%}
- index={{chapter.index}} approx_start={{chapter.approx_start_seconds}}s title: {{chapter.title}}
{% endfor %}

TRANSCRIPT CONTEXT SEGMENTS (each has a stable sequence number and timing):
{% for segment in context_segments -%}
[seq={{segment.sequence_num}} start={{segment.start_time}} end={{segment.end_time}}] {{segment.text}}
{% endfor %}

OUTPUT FORMAT:
Respond with valid JSON only. Do not output analysis, rationale, bullets, or markdown fences.
The first character of your response must be `{` and the last character must be `}`.

For each chapter, identify:
- The segment (by seq) that contains the START of the chapter.
- A short phrase at the START of the chapter: the first 4 words spoken as the chapter begins.

Phrase requirements:
- Each phrase must be a contiguous run of words that appears verbatim in the segment text.
- Prefer phrases fully contained within a single segment.
- Use exactly 4 words when possible. If you cannot, return fewer words (3, 2, or 1) that still appear contiguously.
- Keep each chapter near its approximate start; do NOT jump to an unrelated part of the episode.

Partial output is allowed:
- If you are unsure about a chapter, omit `refined_start_phrase` (or set it to null/empty) and we will keep that chapter's current start.
- Always preserve chapter order and indices.

{
  "chapters": [
    {"chapter_index": 0, "refined_start_segment_seq": 0, "refined_start_phrase": "welcome back to the"}
  ]
}
